{
  "eplquery": "select * from com.ligadata.kamanja.samples.messages where score != -1",

  "query" : {
    "type": "output",
    "messagetype": "com.ligadata.kamanja.samples.messages",
    "source": {
      "type": "where",
      "condition": [ "score!=1" ],
      "source": {
        "type": "input",
        "messagetype": "com.ligadata.kamanja.samples.messages",
        "fields": ["id", "name", "score"]
      }
    }
  }
}