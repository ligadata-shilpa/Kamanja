{
  "eplquery": "select * from com.ligadata.kamanja.samples.messages where score != -1 and score < 100",

  "packagename": "com.ligadata.jtm.test.filter",
  "version": "0.0.1",
  "inputs": [
    { "typename": "com.ligadata.kamanja.samples.messages.msg1" }
  ],

  "filters": [
    {"expression": "score != -1 && score < 100"}
  ],

  "outputs": [
    { "typename":"com.ligadata.kamanja.samples.messages.msg1" }
  ]
}
