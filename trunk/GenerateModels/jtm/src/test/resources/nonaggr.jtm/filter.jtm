{
  "eplquery": "select * from com.ligadata.kamanja.samples.messages where score != -1 and score < 100",

  "package": "com.ligadata.jtm.test.filter",
  "version": "0.0.1",
  "inputs": [
    { "type": "com.ligadata.kamanja.samples.messages" }
  ],

  "filters": [
    {"expr": "score != -1 && score < 100"}
  ],

  "outputs": [
    { "type":"com.ligadata.kamanja.samples.messages" }
  ]
}