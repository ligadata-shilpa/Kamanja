package com.ligadata.pmml.udfs

import org.scalatest._

import scala.collection.mutable.ArrayBuffer

class NotAnyBetweenTests extends FlatSpec {

  /************** ARRAYBUFFER TESTS **************/

  // String Tests
  //"NotAnyBetween" should "return true if no String within a given ArrayBuffer exists within the given range inclusive" in {
    //assert(Udfs.NotAnyBetween(ArrayBuffer("c"."g"), "a", "c", true))

  //}

}
